# MedWaste frontend (Vite). Copy to `.env` or `.env.local`.
# Required for dev. For Capacitor APK: this MUST be set when you run `npm run build`
# (Vercel injects it automatically; local `cap sync` builds often forget it).
VITE_API_URL=https://your-backend.up.railway.app
